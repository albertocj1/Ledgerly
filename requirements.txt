# Core
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# ML
scikit-learn==1.5.2
numpy==2.1.1
joblib==1.4.2

# Optional: real semantic embeddings. The RAG layer falls back to
# TF-IDF+SVD when this isn't installed, so the pipeline still runs in
# locked-down CI. Install for production-quality retrieval.
# sentence-transformers==3.1.1

# Optional: LLM generation. Without a provider the RAG layer returns
# retrieved passages and an explicit "no model configured" notice
# rather than fabricating an answer.
# openai==1.51.0

# Dev
pytest==8.3.3
httpx==0.27.2
